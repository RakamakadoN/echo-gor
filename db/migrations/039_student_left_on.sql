-- 039_student_left_on.sql
-- «Дата ухода» ученика: месяц/день, когда он реально перестал посещать занятия.
-- Отличается от archived_at (когда карточку перевели в архив — это может быть позже).
--   • Ушедший   — задаётся вручную при архивации, редактируется в окне архива.
--   • Отказ     — проставляется автоматически при авто-переносе в архив.
-- Идемпотентно.

ALTER TABLE students ADD COLUMN IF NOT EXISTS left_on DATE;
